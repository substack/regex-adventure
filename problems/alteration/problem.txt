Створiть модуль, що виконує пошук та повертає true в разi, якщо строка починається зi слов `cat`, `dog`, чи `robot`, а далi слiдує чiсло.

Ви можете скористатися наступним шаблоном:

  module.exports = function (str) {
    return /.../.test(str)
  }

Ви можете скористатися оператором `|`, що означеє умову `або`.

Це оператор альтернативи:


  /xyz|qrs|tuv/.test('tuv') // true

Такий оператор можна використовувати в межах группи:

  /0x(ff|00|80)/.test('0x80') // true

Щоб перевiрити ваше рiшення:

  regex-adventure-ua verify solution.js

