А зараз давайте змiнемо наш модуль так, щоб вiн перевiряв чи мiстить строка
слово "LITERALLY", i чи знаходиться це слово на початку речення

Ви можете скористатися наступним шаблоном:

  module.exports = function (str) {
    /* return whether str begins with the string "LITERALLY"
  }

Використовуйте символ `^` щоб позначити початок строки у регулярному виразi.

Чи ви пам'ятаєте, що регулярнi вирази починаются, та закiнчуються символами `/`, а для того, щоб протестувати вираз на вiдповiднiсть до строки, використовують методод `.test()`?

Щоб перевiрити ваше рiшення:

  regex-adventure-ua verify solution.js

