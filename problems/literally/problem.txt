Створiть новий файл з модулем, який приймав би в якостi аругмента строку str та перевiряв чи мiстить ця строка слово "LITERALLY".

Ви можете скористатися цим шаблоном:

  module.exports = function (str) {
    /* return whether str contains the string "LITERALLY"
  }

Для того, щоб задати регулярний вираз, помiстiть його мiж вiдкриваючим `/`, та закриваючим `/` символами. Наприклад, наступна строка:

  /I am a regular expression!/

це регулярний вираз.

Регулярнi вирази в javascript це об'єкти, якi моють методи.
Наприклад, метод `.test()` перевiряє вiдповiднiсть регулярного виразу до строки.

  /wow/.test('so amaze wow such doge') // поверне true

Для проходження цього тесту, запустiть:

  regex-adventure-ua verify solution.js

