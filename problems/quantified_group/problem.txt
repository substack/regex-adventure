Створiть модуль, що повертає true в разi, якщо вхiдна строка має формат 8 колонок hex кодов: 0xFF з одним або декiльками пробiлами посля кожної групи символiв.

HEX коди починаються з символiв `0x` та мають ще два символи великих або маленьких лiтер в дiапазонi `A-F` (`a-f`), або цифри.

Ви можете скористатися наступним шаблоном:

  module.exports = function (str) {
    return /.../.test(str)
  }

Квантифiкатори добре працюють як з группами захоплення так i з пiдмножинами. 
Окрiм вже вiдомих квантифiкаторiв `+`, `*`, та `?` є:

  {n,m}  вiд n до m разiв включно
  {n,}   щонайменьш n разiв
  {,n}   не бiльше n разiв
  {n}    n разiв

Щоб перевiрити ваше рiшення:

  regex-adventure-ua verify solution.js

